{
  "FunctionName": "m5paper-dashboard",
  "Runtime": "provided.al2023",
  "Handler": "bootstrap",
  "Architectures": ["arm64"],
  "MemorySize": 256,
  "Timeout": 60,
  "Role": "{{ must_env `LAMBDA_ROLE_ARN` }}",
  "Environment": {
    "Variables": {
      "TZ": "Asia/Tokyo",
      "DAM_CACHE_FILE": "/tmp/dam_history.json",
      "S3_BUCKET": "{{ must_env `S3_BUCKET` }}",
      "S3_OBJECT_KEY": "{{ must_env `S3_OBJECT_KEY` }}"
    }
  }
}
